import type { Metadata } from 'next'
export const metadata: Metadata = {
  title: 'Social Proof on Landing Pages: What Works and What Wastes Space | Nebula Components',
  description: 'Most landing pages place social proof wrong or use the wrong type. Learn which social proof formats actually lift conversions and where to place them.',
  alternates: { canonical: 'https://nebulacomponents.shop/social-proof-landing-page' },
}
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
