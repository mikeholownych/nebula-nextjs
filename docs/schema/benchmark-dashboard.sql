/**
 * Benchmark dashboard schema
 * (Extensions to existing audits/findings tables)
 */

-- Already using audits and findings tables
-- These are virtual aggregates computed on demand

-- No schema changes required for benchmarks
-- All metrics are computed from existing audits and findings tables
